// * Arquivo de Configuração padrão do Typescript, necessario para que seja feita compilação dos arquivos TS para JS legivel pelo navegador!

// * Propriedade noImplicitAny serve para não permitir que o código seja compilado sem que os tipos sejam atribuidos
{
    "compilerOptions": {
        "target": "ESNext",
        "outDir": "dist/js",
        "noEmitOnError": true,
        "noImplicitAny": true,
        "removeComments": true,
        "strictNullChecks": true,
    },
    "include": [
        "app/**/*"
    ]
}
